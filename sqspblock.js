// Add text to the webpage dynamically
document.addEventListener('DOMContentLoaded', function() {
    var element = document.createElement('p'); // Create a new <p> element
    element.textContent = 'services'; // Set the text content
    document.body.appendChild(element); // Append the element to the body of the document
});
